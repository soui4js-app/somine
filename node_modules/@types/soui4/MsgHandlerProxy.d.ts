import { INativeWnd } from "soui4/INativeWnd";

export class MsgHandlerProxy{
    InstallHook(pNativeWnd:INativeWnd, thisObj:object, fun:object):boolean;
	UninstallHook(pNativeWnd:INativeWnd):void;
}

export class MsgHandle{
    msgHandled:boolean;
}